/**
 * Battle Arena Validation Module. Conforms to the generic validator format
 * @module battle-arena
 */
module.exports = {
    /**
     * @param {state} object - state object to validate
     * @param {cb} function - error first, then new state if valid 
     */
    validate: function (state, cb) {

        

        var err = new Error("not implemented")
        cb(err)





    }
}